# core, needed for the harness
pytest>=7.0

# submission/ is all vectorised numpy
numpy>=1.24

# porter stemmer, purely algorithmic, no nltk_data download needed (matters
# since grading has no network)
nltk>=3.8

# for the optional cython extension
Cython>=3.0

# needed to compile the extension, python slim images don't always ship it
setuptools>=68.0

# only for scripts/download_full_corpus.py
ir_datasets>=0.5.5

# only for harness/generate_baseline.py
rank_bm25>=0.2.2

# optional, cross check against trec_eval
# pytrec_eval>=0.5
