# Core (needed to run the harness and the shipped trivial baseline)
pytest>=7.0

# Required by submission/ — the index, codecs and scorers are all vectorised.
# Explicitly permitted by the assignment (Section 1.2, "numerical libraries
# (NumPy)"). Listed explicitly rather than relied on as a transitive dependency
# of ir_datasets: ir_datasets is only needed for corpus download, which the
# grading image has no reason to install.
numpy>=1.24

# Available if you want to compile part of your submission as a C/C++
# extension (e.g. via Cython) for the efficiency / index-size leaderboard
# components — see docs/SUBMISSION_INTERFACE.md, "Compiled extensions".
# Not required if you stay in pure Python; the provided Docker image has
# the C/C++ toolchain (build-essential) needed to build it either way.
# Using pybind11 instead of Cython is also fine — add it here yourself.
Cython>=3.0

# Needed only for scripts/download_full_corpus.py (fetching the real
# assignment corpus). Not required to run the toy set or the harness itself.
ir_datasets>=0.5.5

# Needed only for harness/generate_baseline.py (regenerating the
# reference BM25 "beat this" run file). A precomputed run file is already
# shipped in data/toy/, so most students will never need to run this.
rank_bm25>=0.2.2

# Optional: if you want to cross-check this harness's metrics against the
# standard TREC tool. Not required.
# pytrec_eval>=0.5
